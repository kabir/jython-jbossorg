To build go to the jython/ directory 
* Modify JBOSS_ORG_PATCH to the patch level, this will be appended onto the raw version '2.5.2.jbossorg-', so if
it JBOSS_ORG_PATCH is '1', the version becomes '2.5.2.jbossorg-1'
* Execute the build-release.sh script

